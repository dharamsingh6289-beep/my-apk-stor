<!DOCTYPE html>
<html lang="hi">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>My APK Store - Download Top Apps & Games</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Google+Sans:wght@400;500;700&family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">

<style>
  :root{
    --bg:#ffffff; --bg-alt:#f6f8fc; --ink:#1a1c1e; --ink-soft:#5f6368;
    --line:#e3e6ea; --accent:#01875f; --accent-dark:#016b4b; --accent-blue:#4285f4; --star:#ffb400;
  }
  *{box-sizing:border-box;margin:0;padding:0;font-family:'Roboto','Segoe UI',sans-serif;}
  body{background:var(--bg);color:var(--ink);padding-bottom:60px;}
  a{color:inherit;text-decoration:none;}

  /* Header */
  header{
    position:sticky;top:0;z-index:20;background:#fff;
    display:flex;align-items:center;gap:15px;
    padding:12px 24px;border-bottom:1px solid var(--line);
  }
  .logo{
    display:flex;align-items:center;gap:8px;font-family:'Google Sans',sans-serif;
    font-size:20px;font-weight:700;white-space:nowrap;cursor:pointer;user-select:none;
  }
  .logo .tri{
    width:24px;height:24px;border-radius:6px;
    background:conic-gradient(from 220deg,#00d4a0,#4285f4,#ffb400,#ea4335,#00d4a0);
    display:flex;align-items:center;justify-content:center;color:#fff;font-size:12px;
  }
  .search-box{
    flex:1;max-width:600px;display:flex;align-items:center;gap:10px;
    background:var(--bg-alt);border-radius:24px;padding:10px 18px;
    border:1px solid var(--line);
  }
  .search-box input{
    border:none;background:transparent;outline:none;width:100%;font-size:14px;color:var(--ink);
  }
  .search-box svg{flex-shrink:0;opacity:.6}

  .owner-badge{
    display:none;background:var(--accent);color:#fff;font-size:11px;font-weight:700;
    padding:4px 10px;border-radius:12px;margin-left:auto;
  }
  .owner-badge.show{display:inline-block;}

  /* Hero banner */
  .hero{
    margin:20px 24px 10px;border-radius:20px;padding:32px 28px;color:#fff;
    background:linear-gradient(120deg,#0c3b2e,#01875f 55%,#00c896);
    display:flex;align-items:center;justify-content:space-between;gap:20px;overflow:hidden;position:relative;
  }
  .hero::after{
    content:"";position:absolute;right:-40px;top:-40px;width:220px;height:220px;border-radius:50%;
    background:rgba(255,255,255,.08);
  }
  .hero h1{font-family:'Google Sans',sans-serif;font-size:26px;margin-bottom:8px;}
  .hero p{font-size:14px;opacity:.9;max-width:460px;}
  .hero .badge{
    background:rgba(255,255,255,.2);display:inline-block;padding:4px 12px;border-radius:20px;
    font-size:12px;margin-bottom:10px;font-weight:600;letter-spacing:.5px;
  }

  /* Category Chips */
  .chip-row{display:flex;gap:10px;overflow-x:auto;padding:10px 24px;}
  .chip{
    flex-shrink:0;padding:8px 18px;border-radius:20px;background:var(--bg-alt);
    font-size:13px;font-weight:500;color:var(--ink-soft);border:1px solid var(--line);cursor:pointer;
  }
  .chip.active{background:var(--accent);color:#fff;border-color:var(--accent);}

  /* Section */
  .section{padding:24px 24px 4px;}
  .section-head{display:flex;justify-content:space-between;align-items:baseline;margin-bottom:16px;}
  .section-head h2{font-family:'Google Sans',sans-serif;font-size:19px;font-weight:700;}

  /* App Card Grid */
  .app-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(160px,1fr));gap:20px;}
  .app-card{
    display:flex;flex-direction:column;position:relative;
    background:var(--bg-alt);padding:14px;border-radius:18px;border:1px solid var(--line);
    transition:transform .15s ease, box-shadow .15s ease;
  }
  .app-card:hover{transform:translateY(-2px);box-shadow:0 4px 12px rgba(0,0,0,.06);}
  .app-icon{
    width:100%;aspect-ratio:1;border-radius:20px;object-fit:cover;margin-bottom:12px;
    background:#fff;display:flex;align-items:center;justify-content:center;font-size:42px;
    border:1px solid var(--line);
  }
  .app-name{font-family:'Google Sans',sans-serif;font-size:14px;font-weight:600;line-height:1.3;margin-bottom:4px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
  .app-meta{font-size:12px;color:var(--ink-soft);margin-bottom:8px;display:flex;align-items:center;gap:4px;}
  .app-meta .stars{color:var(--star);}
  .app-desc-short{font-size:11.5px;color:var(--ink-soft);margin-bottom:12px;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;height:32px;}
  
  .card-actions{display:flex;gap:6px;margin-top:auto;}
  .btn-details{
    flex:1;padding:8px 0;text-align:center;background:var(--bg);color:var(--ink);border-radius:20px;
    font-size:12px;font-weight:600;border:1px solid var(--line);cursor:pointer;
  }
  .download-btn{
    flex:1;padding:8px 0;text-align:center;background:var(--accent);color:#fff;border-radius:20px;
    font-size:12px;font-weight:700;border:none;cursor:pointer;display:block;
  }
  .download-btn:hover{background:var(--accent-dark);}
  
  .del-btn{
    display:none;position:absolute;top:6px;right:6px;background:#ea4335;color:white;
    border:none;width:26px;height:26px;border-radius:50%;cursor:pointer;font-weight:bold;z-index:10;
  }

  /* Support Box */
  .support-box{
    margin:30px 24px 10px;background:var(--bg-alt);border:1px solid var(--line);
    border-radius:18px;padding:20px;text-align:center;
  }
  .support-box h3{font-family:'Google Sans',sans-serif;font-size:16px;margin-bottom:6px;}
  .support-box p{font-size:13px;color:var(--ink-soft);margin-bottom:12px;}
  .support-btn{
    display:inline-block;padding:8px 20px;background:var(--accent-blue);color:#fff;
    border-radius:20px;font-size:13px;font-weight:600;
  }

  /* FAB & Modals */
  .fab{
    position:fixed;bottom:26px;right:26px;width:56px;height:56px;border-radius:50%;
    background:var(--accent);color:#fff;border:none;font-size:26px;cursor:pointer;
    box-shadow:0 4px 14px rgba(0,0,0,.25);display:none;align-items:center;justify-content:center;z-index:20;
  }
  .fab.show{display:flex;}

  .overlay{
    position:fixed;inset:0;background:rgba(0,0,0,.5);
    display:none;align-items:center;justify-content:center;z-index:50;padding:20px;
  }
  .overlay.show{display:flex;}
  .modal{
    background:#fff;border-radius:20px;padding:26px;max-width:440px;width:100%;
    max-height:88vh;overflow-y:auto;
  }
  .modal h3{font-family:'Google Sans',sans-serif;margin-bottom:14px;font-size:18px;}
  .modal label{display:block;font-size:12px;color:var(--ink-soft);margin:10px 0 4px;font-weight:600;}
  .modal input[type="text"], .modal input[type="password"], .modal select, .modal textarea{
    width:100%;padding:10px 12px;background:var(--bg-alt);border:1px solid var(--line);
    border-radius:10px;font-size:13.5px;color:var(--ink);
  }
  .modal input[type="file"]{
    width:100%;padding:8px;background:var(--bg-alt);border:1px dashed var(--line);
    border-radius:10px;font-size:12px;cursor:pointer;
  }
  .modal textarea{resize:vertical;min-height:65px;}
  .modal-btns{display:flex;gap:10px;margin-top:20px;}
  .btn{flex:1;padding:11px;border-radius:20px;border:none;font-weight:700;font-size:13px;cursor:pointer;}
  .btn-submit{background:var(--accent);color:white;}
  .btn-cancel{background:var(--bg-alt);color:var(--ink-soft);border:1px solid var(--line);}

  /* Screenshots Gallery in Details Modal */
  .screenshots-container{display:flex;gap:10px;overflow-x:auto;margin:12px 0;padding-bottom:8px;}
  .screenshots-container img{height:160px;border-radius:10px;object-fit:cover;border:1px solid var(--line);}

  footer{padding:30px 24px;color:var(--ink-soft);font-size:12px;text-align:center;}

  @media(max-width:600px){
    header{padding:12px 16px;}
    .hero{margin:16px;padding:24px 20px;}
    .section{padding:16px;}
  }
</style>
</head>
<body>

<header>
  <!-- Secret Login: Click 3 times on Logo -->
  <div class="logo" id="logoBtn"><div class="tri">▶</div><span>My APK Store</span></div>
  <div class="search-box">
    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#5f6368" stroke-width="2"><circle cx="11" cy="11" r="7"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
    <input type="text" id="searchInput" placeholder="Search apps & games...">
  </div>
  <span class="owner-badge" id="ownerBadge">Owner Mode</span>
</header>

<div class="hero">
  <div>
    <span class="badge">EDITOR'S CHOICE</span>
    <h1>Discover Best Apps & Games</h1>
    <p>Direct download top-rated applications and custom APKs safely.</p>
  </div>
</div>

<div class="chip-row" id="categoryChips">
  <div class="chip active" data-cat="all">All</div>
  <div class="chip" data-cat="game">Games</div>
  <div class="chip" data-cat="app">Apps</div>
  <div class="chip" data-cat="tool">Tools</div>
</div>

<div class="section">
  <div class="section-head"><h2 id="sectionTitle">All Apps</h2></div>
  <div class="app-grid" id="appGrid"></div>
</div>

<!-- Customer Support Section -->
<div class="support-box">
  <h3>Need Help or Customer Support?</h3>
  <p>Facing any issue with downloading or installing apps? Feel free to contact us.</p>
  <a href="mailto:ayush6289134@gmail.com" class="support-btn">📧 Contact Support</a>
</div>

<!-- Floating Action Button for Owner -->
<button class="fab" id="fab" onclick="openAddModal()">+</button>

<!-- Secret Owner Login Modal -->
<div class="overlay" id="loginModal">
  <div class="modal">
    <h3>Owner Login</h3>
    <label>Enter Owner Passcode</label>
    <input type="password" id="passInput" placeholder="Passcode (Default: appadda123)">
    <div class="modal-btns">
      <button class="btn btn-cancel" onclick="closeLoginModal()">Cancel</button>
      <button class="btn btn-submit" onclick="submitLogin()">Login</button>
    </div>
  </div>
</div>

<!-- Add App Modal -->
<div class="overlay" id="addModal">
  <div class="modal">
    <h3>Add New Application</h3>
    <label>App Name</label>
    <input type="text" id="appName" placeholder="e.g. ONE LINE DRAW GAME QUIZ">
    
    <label>Category / Type</label>
    <select id="appType">
      <option value="game">Game</option>
      <option value="app">Mobile App</option>
      <option value="tool">Tool / Utility</option>
    </select>

    <label>App Logo (Select from Gallery)</label>
    <input type="file" id="appIconFile" accept="image/*">

    <label>App Screenshots (Select Multiple from Gallery)</label>
    <input type="file" id="appScreenshotsFile" accept="image/*" multiple>

    <label>Size & Rating</label>
    <div style="display:flex;gap:10px;">
      <input type="text" id="appSize" placeholder="Size (e.g. 18 MB)">
      <input type="text" id="appRating" placeholder="Rating (e.g. 4.8)">
    </div>

    <label>Description</label>
    <textarea id="appDesc" placeholder="Brief description of what this app does..."></textarea>

    <label>Download / APK Link</label>
    <input type="text" id="appLink" placeholder="Direct Download Link (Google Drive / APK URL)">

    <div class="modal-btns">
      <button class="btn btn-cancel" onclick="closeAddModal()">Cancel</button>
      <button class="btn btn-submit" onclick="addNewApp()">Add App</button>
    </div>
  </div>
</div>

<!-- App Details Modal -->
<div class="overlay" id="detailsModal">
  <div class="modal" style="max-width:500px;">
    <div style="display:flex;gap:14px;align-items:center;margin-bottom:14px;">
      <div id="detailIcon" style="width:64px;height:64px;border-radius:16px;background:#f0f2f5;display:flex;align-items:center;justify-content:center;font-size:36px;overflow:hidden;"></div>
      <div>
        <h3 id="detailName" style="margin:0;font-size:18px;"></h3>
        <p id="detailMeta" style="font-size:12px;color:var(--ink-soft);margin-top:2px;"></p>
      </div>
    </div>
    
    <label>Screenshots</label>
    <div class="screenshots-container" id="detailScreenshots"></div>

    <label>About this App</label>
    <p id="detailDesc" style="font-size:13px;line-height:1.5;color:var(--ink-soft);margin-bottom:16px;"></p>

    <a id="detailDownloadBtn" href="#" class="download-btn" style="padding:12px 0;" target="_blank" download>Download APK</a>
    <div style="margin-top:10px;">
      <button class="btn btn-cancel" onclick="closeDetailsModal()">Close</button>
    </div>
  </div>
</div>

<footer>© 2026 My APK Store — Support: ayush6289134@gmail.com</footer>

<script>
const PASSCODE = "appadda123";
let isOwner = sessionStorage.getItem('apk_store_owner') === 'true';
let currentCategory = 'all';

// Initial Default Apps
let apps = JSON.parse(localStorage.getItem('my_apk_store_apps_v3')) || [
  {
    id: "1",
    name: "ONE LINE DRAW GAME QUIZ",
    type: "game",
    icon: "✏️",
    size: "18 MB",
    rating: "4.8",
    screenshots: [],
    link: "https://drive.google.com/uc?export=download&id=1UEY_aJD0PMKuk-yeQ9LmpTr6W08YLprh",
    desc: "Test your brain with ONE LINE DRAW GAME QUIZ! Connect all dots with a single continuous line and solve fun puzzle challenges."
  }
];

// --- RENDER FUNCTION ---
function renderApps() {
  const grid = document.getElementById('appGrid');
  const search = document.getElementById('searchInput').value.toLowerCase();
  
  let filtered = apps;
  if(currentCategory !== 'all'){
    filtered = filtered.filter(a => a.type === currentCategory);
  }
  if(search){
    filtered = filtered.filter(a => a.name.toLowerCase().includes(search) || (a.desc||'').toLowerCase().includes(search));
  }

  if(filtered.length === 0){
    grid.innerHTML = `<div style="grid-column:1/-1;text-align:center;padding:40px;color:var(--ink-soft);">No apps found.</div>`;
    return;
  }

  grid.innerHTML = filtered.map(app => {
    const isBase64OrUrl = app.icon && (app.icon.startsWith('data:') || app.icon.startsWith('http'));
    const iconHtml = isBase64OrUrl 
      ? `<img src="${app.icon}" class="app-icon" alt="${app.name}">`
      : `<div class="app-icon">${app.icon || '📦'}</div>`;

    return `
      <div class="app-card">
        ${isOwner ? `<button class="del-btn" onclick="deleteApp('${app.id}')" style="display:block;">✕</button>` : ''}
        ${iconHtml}
        <div class="app-name">${escapeHtml(app.name)}</div>
        <div class="app-meta"><span class="stars">★</span> ${app.rating || '4.5'} · ${app.size || 'APK'}</div>
        <div class="app-desc-short">${escapeHtml(app.desc || '')}</div>
        <div class="card-actions">
          <button class="btn-details" onclick="openDetails('${app.id}')">Info</button>
          <a href="${escapeHtml(app.link)}" class="download-btn" target="_blank" download>Install</a>
        </div>
      </div>
    `;
  }).join('');

  document.getElementById('fab').classList.toggle('show', isOwner);
  document.getElementById('ownerBadge').classList.toggle('show', isOwner);
}

function escapeHtml(str){
  const d = document.createElement('div'); d.textContent = str ?? ''; return d.innerHTML;
}

// Helper: Convert File to Base64 String
function fileToBase64(file) {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.readAsDataURL(file);
    reader.onload = () => resolve(reader.result);
    reader.onerror = error => reject(error);
  });
}

// --- CATEGORY CHIPS ---
document.querySelectorAll('#categoryChips .chip').forEach(chip => {
  chip.onclick = () => {
    document.querySelectorAll('#categoryChips .chip').forEach(c => c.classList.remove('active'));
    chip.classList.add('active');
    currentCategory = chip.dataset.cat;
    document.getElementById('sectionTitle').textContent = chip.textContent === 'All' ? 'All Apps' : chip.textContent;
    renderApps();
  };
});

// --- SECRET TRIPLE CLICK LOGIN ---
let clickCount = 0;
let clickTimer = null;

document.getElementById('logoBtn').onclick = () => {
  clickCount++;
  if (clickCount === 1) {
    clickTimer = setTimeout(() => { clickCount = 0; }, 1000);
  } else if (clickCount === 3) {
    clearTimeout(clickTimer);
    clickCount = 0;
    if(!isOwner) {
      document.getElementById('loginModal').classList.add('show');
      document.getElementById('passInput').focus();
    } else {
      if(confirm('Turn off Owner Mode?')) {
        isOwner = false;
        sessionStorage.removeItem('apk_store_owner');
        renderApps();
      }
    }
  }
};

function closeLoginModal(){ document.getElementById('loginModal').classList.remove('show'); }
function submitLogin(){
  const val = document.getElementById('passInput').value;
  if(val === PASSCODE){
    isOwner = true;
    sessionStorage.setItem('apk_store_owner', 'true');
    closeLoginModal();
    document.getElementById('passInput').value = '';
    renderApps();
  } else {
    alert('Incorrect passcode!');
  }
}

// --- ADD APP MODAL ---
function openAddModal() { document.getElementById('addModal').classList.add('show'); }
function closeAddModal() { document.getElementById('addModal').classList.remove('show'); }

async function addNewApp() {
  const name = document.getElementById('appName').value.trim();
  const type = document.getElementById('appType').value;
  const size = document.getElementById('appSize').value.trim();
  const rating = document.getElementById('appRating').value.trim();
  const desc = document.getElementById('appDesc').value.trim();
  const link = document.getElementById('appLink').value.trim();

  const iconFileInput = document.getElementById('appIconFile');
  const shotsFileInput = document.getElementById('appScreenshotsFile');

  if(!name || !link) { alert("App Name and Download Link are required!"); return; }

  // Processing App Logo Image
  let iconBase64 = '📦';
  if(iconFileInput.files && iconFileInput.files[0]) {
    iconBase64 = await fileToBase64(iconFileInput.files[0]);
  }

  // Processing Screenshots Images
  let screenshotsBase64 = [];
  if(shotsFileInput.files && shotsFileInput.files.length > 0) {
    for(let i = 0; i < shotsFileInput.files.length; i++) {
      let b64 = await fileToBase64(shotsFileInput.files[i]);
      screenshotsBase64.push(b64);
    }
  }

  apps.push({
    id: Date.now().toString(),
    name, type, icon: iconBase64, size: size || '20 MB', rating: rating || '4.5',
    screenshots: screenshotsBase64, desc, link
  });

  localStorage.setItem('my_apk_store_apps_v3', JSON.stringify(apps));
  closeAddModal();
  
  // Reset Form
  ['appName','appSize','appRating','appDesc','appLink','appIconFile','appScreenshotsFile'].forEach(id => {
    document.getElementById(id).value = '';
  });
  
  renderApps();
}

function deleteApp(id) {
  if(!confirm("Are you sure you want to delete this app?")) return;
  apps = apps.filter(a => a.id !== id);
  localStorage.setItem('my_apk_store_apps_v3', JSON.stringify(apps));
  renderApps();
}

// --- APP DETAILS MODAL ---
function openDetails(id){
  const app = apps.find(a => a.id === id);
  if(!app) return;

  const isBase64OrUrl = app.icon && (app.icon.startsWith('data:') || app.icon.startsWith('http'));
  document.getElementById('detailIcon').innerHTML = isBase64OrUrl 
    ? `<img src="${app.icon}" style="width:100%;height:100%;border-radius:16px;object-fit:cover;">`
    : app.icon || '📦';

  document.getElementById('detailName').textContent = app.name;
  document.getElementById('detailMeta').textContent = `★ ${app.rating || '4.5'} · ${app.size || 'APK'} · ${app.type.toUpperCase()}`;
  document.getElementById('detailDesc').textContent = app.desc || 'No description available.';
  document.getElementById('detailDownloadBtn').href = app.link;

  const shotsContainer = document.getElementById('detailScreenshots');
  if(app.screenshots && app.screenshots.length > 0){
    shotsContainer.style.display = 'flex';
    shotsContainer.innerHTML = app.screenshots.map(url => `<img src="${url}" alt="Screenshot">`).join('');
  } else {
    shotsContainer.style.display = 'none';
  }

  document.getElementById('detailsModal').classList.add('show');
}
function closeDetailsModal(){ document.getElementById('detailsModal').classList.remove('show'); }

document.getElementById('searchInput').addEventListener('input', renderApps);

// Initial Load
renderApps();
</script>
</body>
</html>
